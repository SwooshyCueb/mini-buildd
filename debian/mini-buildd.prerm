#!/bin/sh -e

if [ "${1}" = "remove" ]; then
	# Remove chroots ### 0.8.x deprecated
	/usr/share/mini-buildd/mbd-setup-chroots -d || true
	# Remove auto-lvm setup (if we have it) ### 0.8.x deprecated
	/usr/share/mini-buildd/mbd-setup-lvm -d || true
fi

#DEBHELPER#

exit 0
